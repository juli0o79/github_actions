name: test and deploy
on: [push, repository_dispath]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'  

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: |
          npm run lint
          npm run test

  deploy:
    runs-on: ubuntu-latest
    neeeds: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build project
        run: npm run build
      - name: Deploy to production  
        run: echo "Deploying to production server..."